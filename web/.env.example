# Base URL of the Phase 1 PackageSafe API (same pattern as cli/PACKAGESAFE_API_URL)
PACKAGESAFE_API_URL=http://localhost:8000

# Canonical site URL, used to build absolute OG image URLs
NEXT_PUBLIC_SITE_URL=http://localhost:3000
